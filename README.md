# verticals_mav_metrics

Justin's custom verticals dashboard. Initial scaffold lives on the
`bootstrap/initial-scaffold` branch — open the PR for context, then merge to
land it here.

Stack: Hex -> Neon Postgres -> Vercel / Next.js. See PR body for full setup
steps.
